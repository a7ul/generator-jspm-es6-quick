import React from 'react';
import ReactDOM from 'react-dom';
import {HelloWorld} from './hello-world.jsx';
ReactDOM.render(React.createElement(HelloWorld), document.getElementById('react-container'));
